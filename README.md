# peliculas
React application that consumes the TMDB API to display the latest movie releases. It shows movie posters, titles, genres, and descriptions through a dynamic interface built with reusable components and modern React practices.
